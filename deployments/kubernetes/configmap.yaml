apiVersion: v1
kind: ConfigMap
metadata:
  name: telemedicine-config
  namespace: default
data:
  # NOTE: All environment variables are loaded from root .env file
  # This ConfigMap is populated dynamically by deploy-k8s.sh script
  # Do NOT edit values here - update root .env file instead
  NODE_ENV: "production"
