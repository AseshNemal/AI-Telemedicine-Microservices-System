apiVersion: v1
kind: Secret
metadata:
  name: mongodb-payment-credentials
  namespace: default
type: Opaque
stringData:
  # Template values only. Do not commit real credentials.
  username: "admin"
  password: "change-me"
